# ===== 测试环境前端配置（test 分支，可提交 git）=====
# 测试前端在 3000，后端在 3001；留空则走 /api/backend 反代到 localhost:3001
# 注意：NEXT_PUBLIC_API_URL 在构建时内联，docker-compose 通过 build arg 传入。
NEXT_PUBLIC_API_URL=http://124.223.192.177:3001
